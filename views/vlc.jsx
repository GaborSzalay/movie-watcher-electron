'use babel';

import React from 'react';

export default class VlcSetup extends React.Component {
    render() {
        return (
            <h1>
                VLC!
            </h1>
        );
    }
}